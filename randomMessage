function randomMessage(question){
  let y = ['yes','no','maybe'];
  x=Math.floor(Math.random()*y.length)
  if (x===0) {
    return y[0]
  } else if (x===1) {
    return y[1]
  } else if (x===2){
    return y[2]
  }
}

console.log(randomMessage())
